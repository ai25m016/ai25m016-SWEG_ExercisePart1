name: Backend Tests (Docker image)

on:
  push:
    branches:
      #- 'feature/**'
      #- 'bugfix/**'
      #- 'hotfix/**'
      #- 'release/**'
    paths:
      - 'backend/**'
      - '.github/workflows/backend-docker.yml'
  pull_request:
    branches:
      #- main
      #- develop

jobs:
  # 1) Tests im Docker-Image – läuft für alle oben definierten Events
  test-in-docker:
    name: Backend Tests (Docker)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image for tests
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          load: true
          tags: simple-social-backend:test

      - name: Run backend tests inside container
        run: |
          docker run --rm simple-social-backend:test \
            uv run pytest -q

  # 2) Image bauen & nach GHCR pushen – NUR bei Push auf main
  build-and-push:
    name: Build & Push backend Docker image
    runs-on: ubuntu-latest
    needs: test-in-docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/simple-social-backend:latest
            ghcr.io/${{ github.repository_owner }}/simple-social-backend:${{ github.sha }}
